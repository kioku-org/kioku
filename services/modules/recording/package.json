{
  "name": "@vexa/recording",
  "version": "0.1.0",
  "description": "Recording brick — acquire (PulseAudio/MediaRecorder) + deliver (chunked upload) → recording.v1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "check:isolation": "node scripts/check-isolation.js"
  },
  "dependencies": {
    "playwright": "1.56.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}