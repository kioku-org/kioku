{
  "name": "@vexa/teams-capture",
  "version": "0.1.0",
  "description": "MS Teams capture (browser): voice-level-outline (blue-squares) active-speaker watcher (→ mixed-capture.v1 hints) + chat reader. Mixed audio from @vexa/mixed-capture-core; names resolve downstream.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "check:isolation": "node scripts/check-isolation.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
