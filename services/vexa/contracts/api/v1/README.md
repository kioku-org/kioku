# api.v1 — the public surface (to version at MVP4)

REST + WS + MCP through api-gateway / mcp / meeting-api. Exists today as
OpenAPI schemas + MCP tool schemas; MVP4 versions and publishes them so
proprietary products (and any client) build against published artifacts with
zero monorepo imports. MCP is standard — kept standard.
